let qualquer = 1;
console.log(qualquer, typeof qualquer );
qualquer = 'String';
console.log(qualquer, typeof qualquer );
qualquer = true;
console.log(qualquer, typeof qualquer );
qualquer = [1, 2, 3, 4, 'String', true, 5, 6, 'STRING', false];
console.log(qualquer, typeof qualquer );
qualquer = {
  name: 'Bognar',
  date: new Date(),
  article: 'TypeScript',
  publish: true
};
console.log(qualquer, typeof qualquer );